# =============================================================================
# GreenPulse AI — Frontend environment variable template
# Copy this file to .env and fill in real values for production.
# NEVER commit .env to source control.
# NEVER place private backend credentials or API keys in this file.
# =============================================================================

# The public URL where the backend API is hosted
VITE_API_BASE_URL=https://api.your-backend-domain.com/api
